<Window x:Class="WpfPort.ExternalDatabaseList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfPort"
        mc:Ignorable="d"
        Title="ExternalDatabaseList">
    <Window.Resources>
        <BitmapImage x:Key="HeadBitmapImage" UriSource="Sample-icon.png"/>
        <Image x:Key="MyImage" x:Shared="False" Source="{StaticResource HeadBitmapImage}"/>
        
        <!--<Image x:Key="MyImage" Source ="Sample-icon.png"/>-->
        <SolidColorBrush  x:Key="BackgroundColor">#000000</SolidColorBrush>
        <Style x:Key="placeHolder" TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Grid>
                            <TextBox Text="{Binding Path=Text,
                                                    RelativeSource={RelativeSource TemplatedParent}, 
                                                    Mode=TwoWay,
                                                    UpdateSourceTrigger=PropertyChanged}"
                                     x:Name="textSource" 
                                     Background="Transparent" 
                                     Panel.ZIndex="2" />
                            <TextBox Text="{TemplateBinding Tag}" Background="{TemplateBinding Background}" Panel.ZIndex="1">
                                <TextBox.Style>
                                    <Style TargetType="{x:Type TextBox}">
                                        <Setter Property="Foreground" Value="Transparent"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Text, Source={x:Reference textSource}}" Value="">
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid x:Name="MainGrid" Background="{StaticResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.15*"/>
            <RowDefinition Height="0.15*"/>
           

            <RowDefinition Height="*"/>
            <RowDefinition Height="0.15*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="_File" HorizontalAlignment="Center" HorizontalContentAlignment="Center" >
                <MenuItem Header="_New" />
                <MenuItem Header="_Open" />
                <MenuItem Header="_Save" />
                <Separator />
                <MenuItem Header="_Exit" />
            </MenuItem>
        </Menu>
        <TextBox AcceptsReturn="True" Background="{StaticResource BackgroundColor}"/>
        <Grid x:Name="MenuGrid" DockPanel.Dock="Top" Background="{StaticResource BackgroundColor}" Height="30" HorizontalAlignment="Stretch" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="0.1*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="ExternalGamesButton" Click="ExternalGamesButton_Click" Content="{StaticResource MyImage}" Grid.Column="0" HorizontalContentAlignment="Center" Background="{StaticResource BackgroundColor}" Foreground="White"/>
            <Button x:Name="FullCellButton" Click="FullCellButton_Click" Content="{StaticResource MyImage}" Grid.Column="1" HorizontalContentAlignment="Center" Background="{StaticResource BackgroundColor}" Foreground="White"/>
            <Button x:Name="ExactCellButton" Content="{StaticResource MyImage}" Grid.Column="2" HorizontalContentAlignment="Center" Background="{StaticResource BackgroundColor}" Foreground="White"/>
            <Button x:Name="ManualCodeButton" Content="{StaticResource MyImage}" Grid.Column="3" HorizontalContentAlignment="Center" Background="{StaticResource BackgroundColor}" Foreground="White"/>
            <Button x:Name="CombinatationButton" Content="{StaticResource MyImage}" Grid.Column="5" HorizontalContentAlignment="Center" Background="{StaticResource BackgroundColor}" Foreground="White"/>
            <Button x:Name="UpwardsButton" Content="{StaticResource MyImage}" Grid.Column="6" HorizontalContentAlignment="Center" Background="{StaticResource BackgroundColor}" Foreground="White"/>
            <Button x:Name="SwitchButton" Content="{StaticResource MyImage}" Grid.Column="7" HorizontalContentAlignment="Center" Background="{StaticResource BackgroundColor}" Foreground="White"/>
            <Button x:Name="ClearSearchButton" Content="{StaticResource MyImage}" Grid.Column="8" HorizontalContentAlignment="Center" Background="{StaticResource BackgroundColor}" Foreground="White"/>
            <Button x:Name="SearchButton" Content="{StaticResource MyImage}" Grid.Column="9" HorizontalContentAlignment="Center" Background="{StaticResource BackgroundColor}" Foreground="White"/>
        </Grid>
        <TextBlock x:Name="databaseNameTextBlock" Text="DataBase name" Grid.Row="1" Foreground="White" Background="{StaticResource BackgroundColor}" HorizontalAlignment="Center" VerticalAlignment="Bottom" TextAlignment="Justify" FontSize="20" />

        <Grid x:Name="StatusBarGrid" Grid.Row="2" Background="{StaticResource BackgroundColor}" Height="25" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="0.2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.2*"/>
                <ColumnDefinition Width="0.1*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="AddButton" Content="button" Background="{StaticResource BackgroundColor}" Foreground="white" Grid.Column="0"/>
            <TextBox x:Name="VoucherTextBox" Style="{StaticResource placeHolder}" Tag="Load Voucher here.." Background="{StaticResource BackgroundColor}" Grid.Column="1"/>
            <Button x:Name="UpdateDbButton" Content="Update Db" Grid.Column="4" Background="{StaticResource BackgroundColor}"/>
        </Grid>
        <DataGrid x:Name="DataGrid" Grid.Row="2" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled" RowBackground="{StaticResource BackgroundColor}" Foreground="Green" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{StaticResource BackgroundColor}">
            <DataGrid.Resources>
                <Style TargetType="ScrollBar">
                    <Setter Property="Background" Value="{StaticResource BackgroundColor}" />
                    <Setter Property="BorderBrush" Value="{StaticResource BackgroundColor}" />
                    <Setter Property="Foreground" Value="{StaticResource BackgroundColor}" />
                    <Setter Property="Visibility" Value="Collapsed" />


                </Style>
                <Style BasedOn="{StaticResource {x:Type DataGridColumnHeader}}" TargetType="{x:Type DataGridColumnHeader}">
                    <Setter Property="Background" Value="{StaticResource BackgroundColor}" />
                    <Setter Property="Foreground" Value="Green" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </Style>
            </DataGrid.Resources>
        </DataGrid>
    </Grid>
</Window>